{
  "name": "@repo/ai-service",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python project - copy to container' && exit 0",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check src/",
    "test": "pytest",
    "format": "black src/"
  },
  "dependencies": {},
  "devDependencies": {}
}
